<main>
    <h2>{{firstName}} {{lastName}} </h2>
    <img src="{{profilePicture}}">
    <p><a href="/users/myprofile">Edit Profile</p>
    <div class = "buttonDiv">
        <button type="button" class="submit" onclick="window.location.href='./login'" > Logout </button>
    </div>
</main>
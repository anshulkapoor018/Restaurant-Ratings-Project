window.onload = function() {
    let profile = document.getElementById('profile');
    let login = document.getElementById('login');

    // Check if there is a cookie set for user and make profile visible / change login to logout as well as the link it contains
}